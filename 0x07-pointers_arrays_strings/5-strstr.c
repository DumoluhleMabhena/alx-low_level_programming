#include "main.h"
/**
 * _strstr - fucntion that is needed
 * @haystack: pointer that is parsed
 * @needle: another pointer that is parsed
 * Return: 0 when successful
 */
char *_strstr(char *haystack, char *needle)
{
	return (0);
}
