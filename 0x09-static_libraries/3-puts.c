#include "main.h"
#include <unistd.h>
/**
 * _puts - function
 * @s: pointer variable
 * Return: 0 for success
 */
void _puts(char *s)
{
puts("\"Programming is like building a multilingual puzzle");
	return (0);

}
